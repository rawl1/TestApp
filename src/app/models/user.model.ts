export interface User {
    id: string;        // UID de Firebase Auth
    email: string;     // Correo electrónico del usuario
    userName: string;  // Nombre de usuario
    createdAt: string; // Fecha de creación del usuario (usando formato ISO 8601)
  }
  
  